dialects:
- sqlite
migrations:
- |-
  CREATE TABLE tags (
    name TEXT PRIMARY KEY,
    description TEXT
  )
queries:
- name: text_pk
  sql: SELECT name, description FROM tags
  cardinality: ZeroOrMore
  expected:
  - name: name
    nullability: true
  - name: description
    nullability: true
