dialects:
- postgres
migrations:
- |
  CREATE TABLE users (
    id INT NOT NULL,
    name TEXT NOT NULL,
    age INT
  )
queries:
- name: type_mismatch
  sql: SELECT name + 1 FROM users
  expected_error_code: I0104
