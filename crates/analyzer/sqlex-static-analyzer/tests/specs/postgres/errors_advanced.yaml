dialects:
- postgres
migrations:
- |
  CREATE TABLE users (
    id INT NOT NULL,
    name TEXT NOT NULL,
    age INT
  )
queries:
- name: invalid_function_args
  sql: SELECT SUBSTR(1, 2, 3) FROM users
  expected_error_code: I4108
