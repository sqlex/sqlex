dialect: mysql
migrations:
  - |
    CREATE TABLE users (
      id INT PRIMARY KEY,
      email VARCHAR(255) UNIQUE,
      name TEXT NOT NULL,
      age INT
    )
  - |
    CREATE TABLE order_items (
      order_id INT NOT NULL,
      item_id INT NOT NULL,
      quantity INT NOT NULL,
      PRIMARY KEY (order_id, item_id)
    )
  - |
    CREATE TABLE products (
      sku VARCHAR(50) PRIMARY KEY,
      name TEXT NOT NULL,
      price DECIMAL
    )

queries:
  # Single column primary key with equality should return AtMostOne
  - name: single_pk_equality
    sql: SELECT * FROM users WHERE id = 1
    cardinality: AtMostOne
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # Single column primary key with parameter should return AtMostOne
  - name: single_pk_parameter
    sql: SELECT * FROM users WHERE id = ?
    cardinality: AtMostOne
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # String primary key with equality should return AtMostOne
  - name: string_pk_equality
    sql: SELECT * FROM products WHERE sku = 'ABC123'
    cardinality: AtMostOne
    expected:
      - name: sku
        nullability: false
      - name: name
        nullability: false
      - name: price
        nullability: true

  # Composite primary key with full match should return AtMostOne
  - name: composite_pk_full_match
    sql: SELECT * FROM order_items WHERE order_id = 1 AND item_id = 2
    cardinality: AtMostOne
    expected:
      - name: order_id
        nullability: false
      - name: item_id
        nullability: false
      - name: quantity
        nullability: false

  # Composite primary key with partial match should return Unknown
  - name: composite_pk_partial_match
    sql: SELECT * FROM order_items WHERE order_id = 1
    cardinality: Unknown
    expected:
      - name: order_id
        nullability: false
      - name: item_id
        nullability: false
      - name: quantity
        nullability: false

  # Primary key IN single value should return AtMostOne
  - name: pk_in_single_value
    sql: SELECT * FROM users WHERE id IN (1)
    cardinality: AtMostOne
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # Primary key IN multiple values should return Unknown
  - name: pk_in_multiple_values
    sql: SELECT * FROM users WHERE id IN (1, 2, 3)
    cardinality: Unknown
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # Primary key IS NULL should return AtMostOne (empty result)
  - name: pk_is_null
    sql: SELECT * FROM users WHERE id IS NULL
    cardinality: AtMostOne
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # Primary key with additional AND condition should return AtMostOne
  - name: pk_with_and_condition
    sql: SELECT * FROM users WHERE id = 1 AND name = 'John'
    cardinality: AtMostOne
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true

  # Primary key with OR condition should return Unknown
  - name: pk_with_or_condition
    sql: SELECT * FROM users WHERE id = 1 OR name = 'John'
    cardinality: Unknown
    expected:
      - name: id
        nullability: false
      - name: email
        nullability: true
      - name: name
        nullability: false
      - name: age
        nullability: true
