dialects:
- mysql
migrations:
- |
  CREATE TABLE users (
    id INT NOT NULL,
    name TEXT NOT NULL,
    age INT
  )
queries:
- name: unsupported_full_join
  sql: |
    SELECT u1.id, u2.id
    FROM users u1
    FULL JOIN users u2 ON u1.id = u2.id
