dialects:
- mysql
- postgres
migrations:
- |
  CREATE TABLE users (
    id INT NOT NULL,
    name TEXT NOT NULL,
    age INT
  )
queries:
- name: invalid_group_by
  sql: SELECT name, COUNT(*) FROM users
  expected_error_code: A0107
